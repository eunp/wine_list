<%= form_for @wine do |w| %>

  <% if @wine.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@wine.errors.count, 'error') %> prohibited this wine from being saved:
      </h2>
      <ul>
        <% @wine.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= w.label :name %><br>
    <%= w.text_field :name %>
  </p>

  <p>
    <%= w.label :vineyard %><br>
    <%= w.text_field :vineyard %>
  </p>

  <p>
    <%= w.label :region %><br>
    <%= w.text_field :region %>
  </p>

  <p>
    <%= w.label :category %><br>
    <%= w.text_field :category %>
  </p>

  <p>
    <%= w.label :year %><br>
    <%= w.text_field :year %>
  </p>

  <p>
    <%= w.label :size %><br>
    <%= w.text_field :size %>
  </p>

  <p>
    <%= w.label :price %><br>
    <%= w.text_field :price %>
  </p>

  <p>
    <%= w.label :note %><br>
    <%= w.text_area :note %>
  </p>

  <p>
    <%= w.label :location %><br>
    <%= w.collection_select(:location_id, Location.all, :id, :position, { include_blank: true }) %>
  </p>

  <p>
    <%= w.submit %>
  </p>

<% end %>
